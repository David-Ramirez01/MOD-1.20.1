# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Bajamos un poco el loader a uno más estable para desarrollo
loader_version=0.15.11
loom_version=1.1-SNAPSHOT

# Mod Properties
minecraft_version=1.20.1


maven_group=net.david.mod
archives_base_name=protector-core

# Dependencies
# Esta versión de la API es la más compatible con Yarn 1.20.1
fabric_api_version=0.92.0+1.20.1

mod_version=1.0.0
